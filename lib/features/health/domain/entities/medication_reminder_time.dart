/// Local time-of-day reminder without Flutter dependency.
class MedicationReminderTime {
  const MedicationReminderTime({
    required this.hour,
    required this.minute,
  });

  final int hour;
  final int minute;
}
