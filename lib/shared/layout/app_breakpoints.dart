/// Responsive shell breakpoints — match `docs/ui_screenshots/web/web_shell/code.html`.
const double kMobileBreak = 768;

/// Desktop uses expanded sidebar; tablet uses collapsed by default.
const double kTabletBreak = 1024;
